메소드에서 메인메소드 값 불러올떄
정수값만 불러오는거지 변수자체를 불러오는건 아니다.




*전역변수
클래스 안에는 멤버라는게 존재한다. 두유 리멤버~
static = 전(체영)역 이다. 객체와는 다른 존재...?
Scanner은 메소드의 호출을 위해서 Scanner.nextInt();를 한적은 없다.
이름을 가지는 참조를 만들고 이름.nextInt();를 쓴다.
이와 다르게 static이라는 메소드는 클래스 이름으로 가능하다.
static은 
파이를 쓰고 싶은데 PI는 상수변수로 자바 개발자가 만들어 놓은
상수이므로 가져다 쓴다. 이때 PI값이 있는 클래스 이름이 Math이다.
PI값을 가져오기 위해서 Math클래스의 상수변수PI를 불러온다.
이렇게 불러 올 수 있는 이유는 static이기 때문이다. 즉 전역 변수이기 때문이다

ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

객체 지향 : 모든 현실의 객체(object) -> 객체로 보는 시선에서 시작
객체는 물건,물리적 존재, 아이디어, 생각, 세상에 있는 모든것을 객체로 보는 시각

객체가 뭐냐면? "상태" 와 "동작"이 있다.

객체를 코드로 적고 코드로 만들어진 객체를 통해서
객체들 간에 메세지를 주고 받으며 프로그램의 흐름을 만들어 내는것.

객체 -> 코드
코드 -> 객체

객체 지향 프로그래밍

ex) 강아지를 코드로...?
가짜 강아지를 만든다. 
그 강아지를 여러 형태로 만들어낸다.
객체들 끼리 서로 상호소통 하게 한다.
그 메세지를 서로 주고 받으며 프로그램을 만든다

ex) 값과 기능을 한곳에 모아 놓은것을 객체라 한다.

값/ 상태 -> 멤버변수(member variable / 필드 (fields) 
- 객체의 구성요소

Box를 box1라고 하고
참조 변수도 생겼다
istance
**********************************
public class StaticVariableAndConst3

	double r = Math.random();
		r = (int) (r * 1000); // 이거 약간 이해되는데 설명할 정도는 아님.
		System.out.println(r);
		



instance
박스를 컴파일 해서 바이트코드로 만들어놨다. 다른 클래스 파일에서 프로그램 흐름을 마늘고 있는데
box라는 클래스를 하나의 "box타입"으로 쓸수있다.

참조형은 가르치큰 대상이 하나의 박스이다. box1.new box(); 인스턴스화 한다고 한다.

인스턴스 만든다는건 공간을 할당해준거라고 한다고...?

참조형 변수는 null이란것을 참조할수 있다.
null 참조란 "지금 참조하고 있는게 없다"라는 뜻이다.