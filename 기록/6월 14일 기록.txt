super = 부모의 생성자 

return으로 객체를 만들수도 있고


다른 타입(부모 자식타입)으로 같은 인스턴스를 참조 할 수도 있다.
Book 과 Magazine 이 예시이다. Book이 Magazine의 부모 


Object(객체) 클래스를 우리가 항상 Override해서 사용했다. equal get set toString같은것들!

객체는 가장 최상위의 클래스이다.

항상 toString을 지정하지 않고 사용할때 클래스@숫자dd123123 이런 형태가 나오는 이유는

객체의 toString 형태가 저런 형태이기때문이다.