continue = 
반복문에서 내가 원하는 곳까지만 반복한후 제일 처음으로 돌아가 다시 반복할때
continue를 쓴다.

break = 반복문에서 내가 원하는 곳에서 반복이 아에 멈추게 하는것.


책 이라는 필드를 반복해서 총 5개 만들것이다.

for문을 썼지만 for문 자체는 문장이므로 컴파일 오류가 난다.

필드값을 초기화 해야지만 쓸 수 있다.

필드 값을 초기화 하는것은 초기화기 때문에 생성자이다.

값을 비교한다
배열 N번쨰가 N등이다.



open call hierarchy = 메소드를 쓴 곳들을 확인할 수 있다