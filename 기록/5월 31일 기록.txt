student라는 클래스는 이름, 국,영,수,평균 이라는 정보를 가지고 있다.

우리는 학급을 만들것이다.

학급은 학생 3명을 가지고 있다.
학급은 3명의 이름을 가지는 학급생성자를 만들수 있다.
이 학생들은 똑같이 이름,국,영,수,평균을 가지고 있다.

학급은 학생 3명의 이름을 출력하는 메소드를 가지고 있다.
학급은 총평균을 나타내는 메소드를 가지고 있다.
학급은 점수를 순위로 나타내는 메소드를 가지고 있다.





메인에서 값을 입력 받는다. 우리는 학생의 정보 ( 이름,국,영,수,평균)을 입력받는다

우리는 이 학생들의 



****************************
선언과 초기화는 세트

생성자는 인스턴스 생성될때 마다 호출되는 인스턴스 초기화 '메서드'이다.


기본 생성자는 () 안에 값을 넣지 않고

변수를 하나씩 지정해서 값을 초기화 한다.

생성자 ()안에 바로 값을 넣으면 

