//반지름 값을 입력하면 4/3r*r*r 을 입력한다.
import java.util.Scanner;

public class Sphere{
	public static void main(String [] args){
		Scanner scan = new Scanner(System.in);
		System.out.print("반지름을 입력하세요 : ");
		double radius = scan.nextDouble();
		double sphere = (4 * radius * radius * radius) / 3;
		//숫자는 기본값이 정수형이다. 그렇기때문에 실수형으로 지정 해줘야지
		// 4/3을 계산할수 있다. 또는 계산식에서 실수가 1개만 있어도 
		// 실수형으로 계산이 된다.
		
		System.out.print("구의 부피는 : " + sphere1);
	}
}